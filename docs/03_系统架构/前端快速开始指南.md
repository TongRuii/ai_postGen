# Vue 3 前端快速开始指南

**目标**: 15分钟快速上手 Vue 3 前端开发  
**适合**: 新入职团队成员、初学者

---

## ⏱️ 5分钟快速开始

### 1. 项目初始化 (1分钟)

```bash
# 使用 Vite 创建新项目
npm create vite@latest my-app -- --template vue-ts

cd my-app
npm install
npm run dev
```

### 2. 创建你的第一个组件 (2分钟)

```vue
<!-- src/components/HelloWorld.vue -->
<template>
  <div class="container">
    <h1>{{ message }}</h1>
    <button @click="increment">点击次数: {{ count }}</button>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

const message = ref('欢迎使用 Vue 3!')
const count = ref(0)

const increment = () => {
  count.value++
}
</script>

<style scoped>
.container {
  text-align: center;
  padding: 20px;
}
</style>
```

### 3. 在 App.vue 中使用 (1分钟)

```vue
<template>
  <HelloWorld />
</template>

<script setup>
import HelloWorld from './components/HelloWorld.vue'
</script>
```

### 4. 运行和预览 (1分钟)

```bash
npm run dev
# 访问 http://localhost:5173
```

---

## 📖 核心概念速览（10分钟）

### ref - 响应式数据

```typescript
import { ref } from 'vue'

// 创建响应式数据
const count = ref(0)

// 访问数据（需要 .value）
console.log(count.value) // 0

// 修改数据
count.value = 1

// 模板中自动解包（无需 .value）
// <div>{{ count }}</div>  // 显示 1
```

### computed - 计算属性

```typescript
import { ref, computed } from 'vue'

const count = ref(0)

// 自动缓存计算结果
const doubled = computed(() => count.value * 2)

count.value = 5
console.log(doubled.value) // 10
```

### watch - 监听变化

```typescript
import { ref, watch } from 'vue'

const count = ref(0)

// 监听单个值
watch(count, (newVal, oldVal) => {
  console.log(`count 从 ${oldVal} 变为 ${newVal}`)
})

count.value = 1 // 打印：count 从 0 变为 1
```

### Pinia Store

```typescript
// stores/counterStore.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useCounterStore = defineStore('counter', () => {
  const count = ref(0)
  
  const increment = () => count.value++
  const decrement = () => count.value--
  
  return { count, increment, decrement }
})

// 在组件中使用
<script setup>
const counterStore = useCounterStore()
</script>

<template>
  <div>{{ counterStore.count }}</div>
  <button @click="counterStore.increment">+1</button>
</template>
```

---

## 🔧 常见开发任务

### 任务 1: 处理表单输入

```vue
<template>
  <input v-model="formData.name" placeholder="输入名称">
  <input v-model="formData.email" placeholder="输入邮箱">
  <button @click="submit">提交</button>
</template>

<script setup lang="ts">
import { reactive } from 'vue'

const formData = reactive({
  name: '',
  email: ''
})

const submit = () => {
  console.log(formData)
  // 发送到后端
}
</script>
```

### 任务 2: 列表渲染

```vue
<template>
  <ul>
    <li v-for="item in items" :key="item.id">
      {{ item.name }}
    </li>
  </ul>
</template>

<script setup lang="ts">
import { ref } from 'vue'

const items = ref([
  { id: 1, name: '项目 1' },
  { id: 2, name: '项目 2' }
])
</script>
```

### 任务 3: 条件渲染

```vue
<template>
  <div v-if="isLoading">加载中...</div>
  <div v-else-if="error">错误: {{ error }}</div>
  <div v-else>{{ data }}</div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

const isLoading = ref(false)
const error = ref(null)
const data = ref(null)
</script>
```

### 任务 4: 调用 API

```typescript
// services/projectService.ts
import axios from 'axios'

export const projectService = {
  async getProjects() {
    return axios.get('/api/v1/projects')
  },
  
  async createProject(data) {
    return axios.post('/api/v1/projects', data)
  }
}

// 在组件中使用
<script setup>
import { ref } from 'vue'
import { projectService } from '@/services/projectService'

const projects = ref([])
const isLoading = ref(false)

const loadProjects = async () => {
  isLoading.value = true
  try {
    const { data } = await projectService.getProjects()
    projects.value = data
  } finally {
    isLoading.value = false
  }
}

// 挂载时加载
onMounted(() => loadProjects())
</script>
```

---

## 🚀 常用代码模板

### 模板 1: 列表页面

```vue
<template>
  <div class="list-page">
    <h1>项目列表</h1>
    
    <div v-if="isLoading" class="loading">加载中...</div>
    <div v-else-if="error" class="error">{{ error }}</div>
    <div v-else>
      <ul>
        <li v-for="item in items" :key="item.id">
          {{ item.name }}
        </li>
      </ul>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { projectService } from '@/services'

const items = ref([])
const isLoading = ref(false)
const error = ref(null)

onMounted(async () => {
  isLoading.value = true
  try {
    const { data } = await projectService.getProjects()
    items.value = data
  } catch (err) {
    error.value = (err as Error).message
  } finally {
    isLoading.value = false
  }
})
</script>
```

### 模板 2: 表单页面

```vue
<template>
  <form @submit.prevent="submit">
    <input v-model="form.name" placeholder="名称" required>
    <input v-model="form.email" placeholder="邮箱" type="email" required>
    <button type="submit" :disabled="isSubmitting">
      {{ isSubmitting ? '提交中...' : '提交' }}
    </button>
  </form>
</template>

<script setup lang="ts">
import { reactive, ref } from 'vue'

const form = reactive({
  name: '',
  email: ''
})

const isSubmitting = ref(false)

const submit = async () => {
  isSubmitting.value = true
  try {
    // 调用 API
    await api.post('/submit', form)
  } finally {
    isSubmitting.value = false
  }
}
</script>
```

---

## 🐛 调试指南

### 使用 Vue DevTools

```bash
# 1. 安装浏览器扩展 Vue DevTools
# Chrome: Vue.js devtools

# 2. 打开浏览器开发者工具
# F12 或 右键 -> 检查

# 3. 切换到 Vue 标签
# 查看组件树、Props、State、Events
```

### 常见错误排查

| 错误 | 原因 | 解决方案 |
|------|------|--------|
| `count is not defined` | 忘记 ref | 使用 `ref(0)` 包装 |
| `Cannot read property 'value'` | 忘记 `.value` | 模板自动解包，脚本需要 `.value` |
| 响应式不生效 | 直接修改对象属性 | 使用 `reactive()` 或 `ref()` |
| v-model 不生效 | 忘记处理事件 | 确保组件发出正确的事件 |

---

## 📚 推荐学习路径

### 第 1 周：基础概念
- [ ] ref 和 reactive
- [ ] computed 和 watch
- [ ] 组件基础和模板语法
- [ ] 完成 5 个简单组件

### 第 2 周：组件和路由
- [ ] Props 和 Emit
- [ ] 组件通信
- [ ] 路由配置和导航
- [ ] 实现一个多页应用

### 第 3 周：状态和 API
- [ ] Pinia 状态管理
- [ ] 调用 REST API
- [ ] 错误处理和加载状态
- [ ] 完成一个完整功能模块

### 第 4 周：高级特性
- [ ] 编译器宏（defineProps, defineEmits）
- [ ] 自定义 Hooks（Composables）
- [ ] 性能优化
- [ ] 测试

---

## ❓ 常见问题 (FAQ)

**Q: ref vs reactive 怎么选？**  
A: 基本类型（数字、字符串）用 ref；对象用 reactive。

**Q: 为什么在模板中不需要 .value？**  
A: Vue 在模板中自动解包 ref，但在脚本中需要手动访问 .value。

**Q: 如何在组件间共享状态？**  
A: 使用 Pinia Store。

**Q: 异步操作如何处理？**  
A: 使用 async/await，配合 isLoading 状态。

**Q: 如何跳转页面？**  
A: 使用 Vue Router 的 `useRouter().push('/path')`。

---

## 🎯 下一步

完成快速开始后，建议：

1. **阅读完整文档** - `前端系统架构_Vue3综合文档.md`
2. **查看代码示例** - 前端代码示例_Vue3完整实现.md
3. **配置开发环境** - 前端配置和工具链.md
4. **实践项目** - 开始开发实际功能

---

**版本**: V1.0  
**最后更新**: 2025年10月26日
